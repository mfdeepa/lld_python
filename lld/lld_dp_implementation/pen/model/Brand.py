from enum import Enum, auto

class Brand(Enum):
    PARKER = auto
    WATERMAN = auto
    MONTEGRAPPA = auto
    PILOT= auto
    CROSS_PEN = auto
    MONTBALC = auto
    PAPERMATE = auto
    CROSS = auto
    CELLO = auto
    WRITOMETER = auto
    LAMY = auto
    FABER_CASTELL = auto

